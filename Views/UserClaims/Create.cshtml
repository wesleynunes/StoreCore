@model StoreCore.Data.ApplicationUserClaim

@{
    ViewData["Title"] = "Cadastrar reivindicações";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h1>Cadastrar</h1>
<h4>Reivindicações</h4>
<hr />

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item active"><a asp-action="index">Voltar</a></li>
        <li class="breadcrumb-item"><a asp-action="index" asp-controller="Admin">Painel</a></li>
    </ol>
</nav>

<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserId" class="control-label">Usuário</label>
                <select asp-for="UserId" class="form-control" asp-items="ViewBag.UserId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ClaimType" class="control-label">Tipo de reivindicação</label>
                @*<input asp-for="ClaimType" class="form-control" />*@
                <select class="form-control" asp-for="ClaimType" asp-items="@new  SelectList(Enum.GetNames(typeof(StoreCore.Models.admin.ClaimType)))"></select>
                <span asp-validation-for="ClaimType" class="text-danger"></span>
            </div>       
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">Voltar</a>
            </div>
        </form>
    </div>
</div>



